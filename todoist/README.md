# Todoist

Create, find, and complete Todoist tasks. Runtime injects `${TODOIST_API_TOKEN}` — never commit secrets. Category stub.

**Category:** Productivity  
**Auth:** `secret` via `TODOIST_API_TOKEN`  
**Wired:** false

Account-owned auth stays product-side. `mcp.json` uses `${ENV}` placeholders only.
