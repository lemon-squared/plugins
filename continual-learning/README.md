# Continual learning

First-party Runtime skill + agent brief that mines person transcripts and updates durable memory.

Orchestration stays in the `continual-learning` skill; writes happen in the `memory-updater` brief against person/account memory and agent transcripts.
